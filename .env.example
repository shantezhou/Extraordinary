# Copy this file to `.env.local` and fill in a real key.
# Used by both /api/transcribe (Whisper) and /api/generate (GPT-4o).
# Server-side only — never prefix with NEXT_PUBLIC_.

OPENAI_API_KEY=sk-...
